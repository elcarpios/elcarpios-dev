---
import Icon from "@components/Icon.astro";

interface Props {
  logo: string;
  link: string;
}

const { logo, link } = Astro.props;
---

<a href={link} class="flex justify-center items-center">
  <Icon
    name={logo}
    class="px-2 w-48 hover:text-gray-900 dark:hover:text-white"
  />
</a>
